# Route budget report

Generated by `npm run budgets` (`web/scripts/route-budgets.mjs`, WO-23) at 2026-08-29T17:24:21.422Z.
Next.js 16.3.3 production build (`next build --webpack`).
Budgets: `web/budgets.json` — RC-01, the seven-row reconciled budget table in docs/revamp/06-WORK-ORDERS.md §1, ratified at Gate 2 (docs/revamp/DECISIONS.md D-010).

**Result: pass.** Every gated ceiling holds.

## Gated rows

These are the ceilings `npm run budgets` enforces. Any breach exits non-zero.

| Row | Measured | Budget | Headroom | Retained baseline | Δ baseline | Status |
|---|---:|---:|---:|---:|---:|:---|
| `/` first-load JS, excl. polyfill | 158,880 B (155.2 KiB) | 166,912 B (163.0 KiB) | +8,032 B | 137,272 B (134.1 KiB) | +21,608 B | pass |
| `/c/[id]` first-load JS, excl. polyfill | 182,814 B (178.5 KiB) | 192,512 B (188.0 KiB) | +9,698 B | 184,745 B (180.4 KiB) | −1,931 B | pass |
| Shared framework/runtime chunk | 131,642 B (128.6 KiB) | 139,264 B (136.0 KiB) | +7,622 B | — | — | pass |
| All emitted CSS | 9,604 B (9.4 KiB) | 11,264 B (11.0 KiB) | +1,660 B | 4,288 B (4.2 KiB) | +5,316 B | pass |
| All self-hosted font files (woff2, latin subset) | 103,476 B (101.1 KiB) | 109,568 B (107.0 KiB) | +6,092 B | 0 B (0.0 KiB) | +103,476 B | pass |

## Rows this script does NOT gate

Listed so the ceiling is never silently treated as enforced. `npm run budgets` cannot pass or fail on these rows.

| Row | Budget | Status | Enforced by |
|---|---:|:---|:---|
| Total transferred JS on a settled report route, incl. lazy chunks | 245,760 B (240.0 KiB) | **not gated here** | WO-21 -- a Playwright network-transfer assertion on a settled report route, in the per-PR chromium job |

> Total transferred JS on a settled report route, incl. lazy chunks: NOT MEASURED HERE. This row cannot come from the build manifests — lazy chunks are fetched at runtime. It is enforced by WO-21 -- a Playwright network-transfer assertion on a settled report route, in the per-PR chromium job.

## Reported, not gated

| Row | Measured | Reference ceiling | Δ reference | Composition |
|---|---:|---:|---:|:---|
| Derived: total first-load transfer for `/c/[id]`, cold cache | 295,894 B (289.0 KiB) | 313,344 B (306.0 KiB) | +17,450 B | route-js-conversation + emitted-css + self-hosted-fonts |

RC-01: the derived row is a **transfer ceiling, not an LCP ceiling** — fonts load `font-display: swap`, are cached across both routes, and do not block first paint. It is here so a reviewer sees the real page weight; the gate stays on the per-class rows.

## Baseline reproduction

WO-23 criterion 6: the script's own correctness proof is that it reproduces the retained `docs/revamp/baseline/README.md` figures from the current build.

| Row | Retained baseline | Measured now | Reproduced to the byte |
|---|---:|---:|:---|
| `/` first-load JS, excl. polyfill | 137,272 B | 158,880 B | no (+21,608 B) |
| `/c/[id]` first-load JS, excl. polyfill | 184,745 B | 182,814 B | no (−1,931 B) |
| All emitted CSS | 4,288 B | 9,604 B | no (+5,316 B) |
| All self-hosted font files (woff2, latin subset) | 0 B | 103,476 B | no (+103,476 B) |

Where a row is not byte-exact, the residue is accounted for rather than absorbed into the budget:

- **`/` (+1 B).** The `/` and `/c/[id]` unions share six chunks and differ only in `app/page-*.js` versus `454-*.js` + `app/c/[id]/page-*.js`. `/c/[id]` reproduces exactly, so the method and the gzip settings are proved; the residue is confined to the one chunk `/` does not share. The retained baseline was captured from source commit `e6e8739`, which is not an ancestor of this repository's history, so a one-byte compressed difference in the home page chunk is a tree difference, not a measurement difference.
- **CSS (−21 B).** 4,267 + 21 = 4,288 exactly, where 21 is the length of a Next stylesheet filename plus its NUL terminator (`<16 hex>.css` is always 20 characters). That is the `FNAME` header `gzip(1)` writes when it compresses a named file and omits with `-n`: `gzip -c 036cc441629251b0.css` is 4,288 bytes and `gzip -cn` is 4,267. The retained CSS figure therefore includes 21 bytes of gzip filename metadata that no server ever sends. This script measures payload, so it reports 4,267 for every row and does not reproduce the header. The route rows confirm the split: with `FNAME` headers the `/c/[id]` union would be 184,928 B, not the retained 184,745 B.

## Manifest cross-check

Next 16.3.3 no longer emits `.next/app-build-manifest.json`, so the app-router route → chunk association is rebuilt from `build-manifest.json` (`rootMainFiles`, `polyfillFiles`), `app-path-routes-manifest.json`, and each entry's `page_client-reference-manifest.js`. For every statically prerendered route the derived union is compared with the `<script src>` set Next actually wrote into the HTML:

| Route | Cross-check |
|---|:---|
| `/` | skipped — server-rendered on demand — the per-request CSP nonce (WO-30) makes this route dynamic, so Next writes no HTML to compare against |
| `/c/[id]` | skipped — dynamic route segment — no single prerendered HTML exists |

## Chunk detail

### `/` first-load JS, excl. polyfill

| File | Gzip | Raw |
|---|---:|---:|
| `static/chunks/214-9ffe0eda076e6223.js` | 3,357 B | 9,071 B |
| `static/chunks/318-51ffdd5b3b0e38e5.js` | 7,708 B | 25,889 B |
| `static/chunks/363-148756a5c34a9b78.js` | 5,927 B | 16,128 B |
| `static/chunks/4bd1b696-92152b0f5947070d.js` | 63,370 B | 201,054 B |
| `static/chunks/756-1a2408fa36a3ab4b.js` | 3,617 B | 8,965 B |
| `static/chunks/794-aa61c1a5715b645c.js` | 65,603 B | 240,795 B |
| `static/chunks/945-b1948d70c3bf1b56.js` | 2,497 B | 5,519 B |
| `static/chunks/app/(workspace)/error-a08f436b6165048a.js` | 325 B | 463 B |
| `static/chunks/app/(workspace)/layout-ec510b6bd0f58dba.js` | 176 B | 233 B |
| `static/chunks/app/(workspace)/page-87c247d4b00125ec.js` | 2,149 B | 4,759 B |
| `static/chunks/app/global-error-aa2b022d9feb1940.js` | 963 B | 1,844 B |
| `static/chunks/app/layout-1c092d2117c90dcd.js` | 322 B | 744 B |
| `static/chunks/app/not-found-345cd4b281a971af.js` | 197 B | 321 B |
| `static/chunks/main-app-6e7151ae6e363ba0.js` | 216 B | 469 B |
| `static/chunks/webpack-9a9296bbba1de6d9.js` | 2,453 B | 5,056 B |
| **total** | **158,880 B** | |

### `/c/[id]` first-load JS, excl. polyfill

| File | Gzip | Raw |
|---|---:|---:|
| `static/chunks/109-57ae3e34cf47bd24.js` | 11,173 B | 38,009 B |
| `static/chunks/214-9ffe0eda076e6223.js` | 3,357 B | 9,071 B |
| `static/chunks/318-51ffdd5b3b0e38e5.js` | 7,708 B | 25,889 B |
| `static/chunks/363-148756a5c34a9b78.js` | 5,927 B | 16,128 B |
| `static/chunks/4bd1b696-92152b0f5947070d.js` | 63,370 B | 201,054 B |
| `static/chunks/756-1a2408fa36a3ab4b.js` | 3,617 B | 8,965 B |
| `static/chunks/794-aa61c1a5715b645c.js` | 65,603 B | 240,795 B |
| `static/chunks/894-7954b8342692db92.js` | 1,617 B | 3,263 B |
| `static/chunks/945-b1948d70c3bf1b56.js` | 2,497 B | 5,519 B |
| `static/chunks/app/(workspace)/c/[id]/error-0a5e4b530bbd97c3.js` | 324 B | 459 B |
| `static/chunks/app/(workspace)/c/[id]/page-0102545e8d579238.js` | 12,969 B | 39,022 B |
| `static/chunks/app/(workspace)/error-a08f436b6165048a.js` | 325 B | 463 B |
| `static/chunks/app/(workspace)/layout-ec510b6bd0f58dba.js` | 176 B | 233 B |
| `static/chunks/app/global-error-aa2b022d9feb1940.js` | 963 B | 1,844 B |
| `static/chunks/app/layout-1c092d2117c90dcd.js` | 322 B | 744 B |
| `static/chunks/app/not-found-345cd4b281a971af.js` | 197 B | 321 B |
| `static/chunks/main-app-6e7151ae6e363ba0.js` | 216 B | 469 B |
| `static/chunks/webpack-9a9296bbba1de6d9.js` | 2,453 B | 5,056 B |
| **total** | **182,814 B** | |

### Shared framework/runtime chunk

| File | Gzip | Raw |
|---|---:|---:|
| `static/chunks/4bd1b696-92152b0f5947070d.js` | 63,370 B | 201,054 B |
| `static/chunks/794-aa61c1a5715b645c.js` | 65,603 B | 240,795 B |
| `static/chunks/main-app-6e7151ae6e363ba0.js` | 216 B | 469 B |
| `static/chunks/webpack-9a9296bbba1de6d9.js` | 2,453 B | 5,056 B |
| **total** | **131,642 B** | |

### All emitted CSS

| File | Gzip | Raw |
|---|---:|---:|
| `static/css/57ead69bc70c225b.css` | 1,005 B | 3,488 B |
| `static/css/7cd7f3878632625c.css` | 641 B | 1,528 B |
| `static/css/b4bae9b1da72d7a0.css` | 1,102 B | 3,995 B |
| `static/css/f8fb8d84349ca80d.css` | 6,856 B | 30,430 B |
| **total** | **9,604 B** | |

### All self-hosted font files (woff2, latin subset)

| File | Gzip | Raw |
|---|---:|---:|
| `.next/static/media/4a7f5dc07e8cb505.woff2` | 14,276 B | 14,276 B |
| `.next/static/media/6f76abcab61531c7.woff2` | 20,896 B | 20,896 B |
| `.next/static/media/8b355a0d3365113c.p.woff2` | 19,892 B | 19,892 B |
| `.next/static/media/b75e973a7f03466f.woff2` | 34,416 B | 34,416 B |
| `.next/static/media/c9067fc2f3d1f4e5.woff2` | 13,996 B | 13,996 B |
| **total** | **103,476 B** | |

## Method

- Compression: gzip, zlib level 6 (zlib's default), one gzip stream per file, summed.
- The level is not a preference: summing per-file level-6 gzip over the `/c/[id]` first-load union reproduces the retained baseline's 184,745 B exactly, and no other level does.
- Polyfills are excluded from every JS row (RC-01 says "excl. polyfill").
- Fonts are measured as raw woff2 bytes, not gzipped: woff2 carries its own Brotli compression, so the file size is the wire weight.
- Stylesheets are gzipped like JS and summed across every emitted `.css`.

## Ratchet rule

04-ARCHITECTURE.md §8.4: budgets may only move in a PR that says why in the PR body and updates `web/budgets.json` in the same commit. `scripts/route-budgets.mjs` accepts no command-line arguments and reads no environment variables, so no flag and no env var can skip this check; `tests/budgets.test.ts` asserts that against the script's own source.

### Ceilings that have moved

Every deviation from the ratified RC-01 table, so a raised ceiling is visible on every run rather than buried in a merged diff.

| Row | From | To | Date | Authority |
|---|---:|---:|---|---|
| route-js-home | 167,936 B | 166,912 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |
| route-js-conversation | 199,680 B | 192,512 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |
| shared-framework-runtime | 141,312 B | 139,264 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |
| emitted-css | 12,288 B | 11,264 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |
| self-hosted-fonts | 122,880 B | 109,568 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |
| derived-total-first-load | 334,848 B | 313,344 B | 2026-08-29 | WO-31 acceptance criterion 5: 'Budget thresholds are ratcheted down to the measured post-cleanup values, under the same ratchet rule.' Every figure below was measured by scripts/route-budgets.mjs on this branch rebased onto main at d3460a7 (PR #111). To be recorded in docs/revamp/DECISIONS.md by the coordinator. |

- **route-js-home** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. 166912 B (163 KiB) is +5.1% over the measurement, holding WO-20's convention of a deliberately tighter figure than RC-01's ~8% for a row measured rather than projected. baselineBytes stays 137272 -- the retained Gate 1 figure for the LEGACY landing page -- so the report's baseline-reproduction table keeps meaning what it meant. RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.
- **route-js-conversation** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. This is the row with real slack to bank: 199680 B was RC-01's +8.1% over a baseline of 184745 B measured on the LEGACY ConversationThread, and the finished /c/[id] is 182776 B -- below the legacy baseline itself, because WO-20's composition took the static react-markdown import off the route. 192512 B (188 KiB) is +5.3% over the measurement. baselineBytes stays 184745 for the same reason route-js-home's stays 137272. RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.
- **shared-framework-runtime** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. 131641 B against WO-23's 130865 B: the row grew 779 B when WO-30 added the nonce middleware, and nothing in WO-31 touches it -- React DOM and the Next app-router client runtime are not ours to cut. 139264 B (136 KiB) is +5.8%, which is less headroom than WO-23's 8.0% and is a deliberate trade: this is the row most exposed to dependency drift (`next` is a caret range), so it keeps the widest margin of the three JS rows while still ratcheting down. RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.
- **emitted-css** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. 11264 B (11 KiB) is +18.5%, and this is the one row that deliberately keeps a wide percentage. Two reasons, both about absolute size rather than proportion. The row is small, so +18.5% is 1757 B -- a handful of declarations, not a licence. And the headroom was sized against `fix/mobile-shell-cls-lcp` before it merged, on the reasoning that a hairline CSS ceiling would block a Gate-3 repair rather than catch a regression. That fix has since merged as #111 and is inside this measurement: it cost 17 B. RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.
- **self-hosted-fonts** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. 109568 B (107 KiB) is +5.9% over the measured woff2 faces. RC-01's 120 KiB was set when the row reported 0 B and was flagged not-yet-meaningful; WO-02's faces have landed, so the ceiling can finally be set against binaries that exist. A font row moves only when a FACE or a subset changes -- the pending shell fix touches only `preload`, which changes no emitted file -- so this row needs no headroom for work in flight. RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.
- **derived-total-first-load** — THE DELETIONS SHIPPED ALMOST NO BYTES, AND THAT IS THE FINDING RATHER THAN A DISAPPOINTMENT. Measured against WO-20's figures on the SAME main (8f0d738), `/` moves 158899 -> 158839 B (-60) and `/c/[id]` 182784 -> 182723 B (-61) across this PR; the finals below sit 39 B and 53 B above those, which is #111's cost and not this PR's. The deletions are nearly free because WO-20 had already stopped COMPOSING the nine legacy components and the bundler had already tree-shaken them -- what WO-31 removes is dead FILES, not shipped weight. So this ratchet is not banking a saving this PR made. It closes the gap between ceilings that were projected from the LEGACY surface and the finished surface that is now what ships -- the one Gate-4 moment where every row is measured against the thing it gates. Not an independent decision: this row is defined as the sum of route-js-conversation, emitted-css and self-hosted-fonts, and 192512 + 11264 + 109568 = 313344 B (306 KiB) exactly, as 199680 + 12288 + 122880 = 334848 did before. It moves because its three components moved. It is reported, never gated (RC-01, WO-23 criterion 5). RE-MEASURED AFTER A REBASE. This PR's first measurement was taken on main at 8f0d738; `fix/mobile-shell-cls-lcp` then merged as #111 and the finals were retaken on the rebased tree at d3460a7. That fix costs `/` +39 B, `/c/[id]` +53 B and the CSS row +17 B -- its ~33 new declarations gzip to almost nothing -- and the font row not one byte, because it changes `preload` and emits no new face. Every ceiling below already carried that movement.

## Not covered by this report

- **CLS (RC-06).** The CI gate is ≤ 0.02 and the design intent and measured baseline are 0.000. CLS is measured by WO-21 (Playwright) and WO-29 (Lighthouse CI), not here. RC-06 requires any state that lands non-zero to carry a written justification in this report; no such justification has been filed, and none is due until those measurements exist.
- **Lab performance targets (04 §8.2).** WO-29, nightly `lhci autorun`.
- **Reflow (`scrollWidth <= clientWidth`).** WO-21, per-PR chromium E2E.

