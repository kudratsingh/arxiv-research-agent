{
  "schema_version": 1,
  "fixture_sets": [
    {
      "name": "hand_authored_session_plans",
      "status": "complete",
      "directory": "session_plans",
      "fixture_kind": "hand-authored",
      "content_kind": "session_plan",
      "blocked_on": "",
      "completion_condition": "",
      "description": "Session plans written by hand as fixed inputs for WO-W09's plan-coherence judge. Includes the honest_downscope / budget_ignoring pair for the same 10-minute scenario: the judge must score them differently. The budget_ignoring variant is deliberately dishonest and could never be recorded from a working system."
    },
    {
      "name": "hand_authored_transcripts",
      "status": "complete",
      "directory": "transcripts",
      "fixture_kind": "hand-authored",
      "content_kind": "transcript",
      "blocked_on": "",
      "completion_condition": "",
      "description": "Session transcripts written by hand to exercise the assessment-honesty rules without a session graph: an unassessed close, an evidence-linked assessment, and an injection that must not reach a control field."
    },
    {
      "name": "recorded_mock_session_transcripts",
      "status": "pending",
      "directory": "recorded_mock_sessions",
      "fixture_kind": "recorded-mock",
      "content_kind": "transcript",
      "blocked_on": "WO-W03",
      "completion_condition": "WO-W03's session graph is merged, so a scenario can be replayed through `build_session_workflow()` under `use_mock_data=true` with ANTHROPIC_API_KEY=local-preview-disabled. Record one transcript per scenario in src/eval/learning_benchmark.LEARNING_SCENARIOS, stamp each provenance header with the generating commit and mock_mode=true, then flip this entry's status to 'complete' and clear blocked_on / completion_condition. Until then this directory MUST stay empty: src/eval/learning_fixtures.validate_fixtures fails if a pending set holds files, so no hand-written transcript can be passed off as a recording.",
      "description": "Deterministic transcripts recorded from the session graph's mock mode. The WO-W08 soft completion gate (05-WEDGE-WORK-ORDERS.md §5.1, the dashed W03 -> W08 edge)."
    }
  ]
}
