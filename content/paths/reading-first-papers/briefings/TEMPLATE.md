<!-- provenance
{
  "resource_id": "arxiv:0000.00000",
  "path_id": "reading-first-papers",
  "provenance": "generated",
  "label": "generated, human-reviewed",
  "briefing_version": 1,
  "generated_by": "<job_id of the POST /research run that drafted this>",
  "generated_at": "<YYYY-MM-DD>",
  "reviewed_by": null,
  "reviewed_at": null
}
-->

> **Generated by the learning agent, reviewed by \<name\>.** Replace
> \<name\> at review time, and set `reviewed_by` / `reviewed_at` in the
> header above in the same edit. The manifest entry's `reviewed_by` must
> match this file's, or the path will not load.

## Why this paper, why now

One paragraph, 120–180 words. Name the previous entry, name the question
it left open, and say what this paper does about it. This is the only
place the path speaks about its own ordering.

## Read closely, skim, skip

Keyed to the sections `src/tools/chunker.py` detects — Introduction,
Method, Results, and so on — so the guidance points at structure rather
than at page numbers.

| Section | What to do | Why |
|---|---|---|
| | | |

## Vocabulary you need first

Terms the reader must already hold to get through the close-read
sections. Three to six of them; the same list goes on the manifest
entry's `vocabulary` field.

- **term** — one-sentence gloss.

## What it got wrong, and what superseded it

The temporal-honesty section. Say plainly what has not survived, date the
claim, and link onward to the entry that revises it. A path without this
section is a bibliography.

---

The paper itself: <https://arxiv.org/abs/0000.00000> — link out, never
re-host.

<!--
Format rules this file has to keep, all enforced by
`src/content/schema.py` and asserted by `tests/test_content_manifest.py`:

  * The provenance header is the first thing in the file, is valid JSON,
    and names the run that produced it (`generated_by`) whenever
    `provenance` is `generated`.
  * The first visible line is the display label as a bold block quote —
    02 §3.2 puts the label at the point of display, not in a footer.
  * No link may point at a PDF, an e-print, or YouTube.
  * Block quotes are attributed on the line that follows them, and the
    quoted material stays under 1,200 characters and 15% of the file.
-->
